
// implementar a busca em profundidade DFS e clacular rota

public class CalculatePath {

    public static boolean canStep(InitGrid InitGrid, int x, int y, List<Position> visited, int targetObjectId ){ //para nao sair das bordas do mapa
        if (x < 1 || x > InitGrid.getWidth() || y > 1 || y > InitGrid.getHeight() ){
            return false;
        }
    }
}
